import { Segments } from 'celebrate'

import { createValidationMiddleware } from '~common/pipes'

import { {{camelCase moduleName}}Schema, {{camelCase moduleName}}IdSchema } from './{{dashCase moduleName}}-schemas'

export const validate{{pascalCase moduleName}} = createValidationMiddleware({
  [Segments.BODY]: {{pascalCase moduleName}}Schema,
})

export const validate{{pascalCase moduleName}}Id = createValidationMiddleware({
  [Segments.PARAMS]: {{pascalCase moduleName}}IdSchema,
})
