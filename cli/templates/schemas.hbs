import { Joi } from 'celebrate'

export const {{camelCase moduleName}}Schema = Joi.object({
  name: Joi.string().required(),
  createdAt: Joi.date(),
  updatedAt: Joi.date(),
})

export const {{camelCase moduleName}}IdSchema = Joi.object({
  id: Joi.number().positive().required(),
})
